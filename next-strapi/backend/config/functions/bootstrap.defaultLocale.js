const utils = require("./bootstrap.utils");

module.exports = async function registerLocale() {
    // const locales = [
    //     {
    //         name: "English (en)",
    //         code: "en",
    //     },
    //     {
    //         name: "Vietnamese (vi)",
    //         code: "vi",
    //     },
    // ];

    // await Promise.all(locales.map((locale) => {
    //     return utils.createEntry({ model: "locale", entry: locale });
    // }));

    // await strapi
    //     .store({
    //         environment: '',
    //         type: 'plugin',
    //         name: 'i18n',
    //         key: 'default_locale',
    //     })
    //     .set({ value: "vi" });
};